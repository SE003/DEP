<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Blog</title>
</head>
<body>
    <h1>Edit Blog</h1>
    <form action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
        <label for="title">Title:</label>
        <input type="text" name="blog[title]" id="title" value="<%= blog.title %>" required><br>

        <label for="image">Image URL:</label>
        <input type="text" name="blog[image]" id="image" value="<%= blog.image %>"><br>

        <label for="body">Body:</label>
        <textarea name="blog[body]" id="body" rows="4" cols="50" required><%= blog.body %></textarea><br>

        <button type="submit">Update</button>
    </form>
    <a href="/blogs/<%= blog._id %>">Cancel</a>
</body>
</html>
